specVersion: "v1beta"
serviceId: "event-planner"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "event-planner-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
